Constants
---------
- A constant is basically a variable
    - Whose assigned value can NOT be changed

    - It is considered best practice to use a constant instead of a variable
        - When the assigned value is NOT expected to be changed 
