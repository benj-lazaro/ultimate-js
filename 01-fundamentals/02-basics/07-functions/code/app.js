// Functions

// Declaring a function with the parameter variables
function greet(firstName, lastName) {
  console.log("Hello " + firstName + " " + lastName + "!");
}

// Calling the function with an argument value
greet("John", "Wick");
